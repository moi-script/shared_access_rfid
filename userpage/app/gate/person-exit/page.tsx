import GateTerminal from "@/components/gate/GateTerminal";

export const metadata = { title: "Person Exit · Gate Terminal" };

export default function Page() {
  return <GateTerminal routeId="person-exit" />;
}

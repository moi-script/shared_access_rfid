import GateTerminal from "@/components/gate/GateTerminal";

export const metadata = { title: "Vehicle Exit · Gate Terminal" };

export default function Page() {
  return <GateTerminal routeId="vehicle-exit" />;
}
